# Operations & Deployment Scripts

This directory contains operations scripts:

- `backup.sh`: Automated PostgreSQL dump script for `medusa_db` and `strapi_db`.
- `healthcheck.sh`: Stack health monitoring script.
- `seed.sh`: Baseline catalog & CMS seeding.
