# Docker Infrastructure

This directory holds Docker configurations for local development and single-VPS production:

- `docker-compose.dev.yml` (Task 02)
- `docker-compose.prod.yml` (Task 38)
- `Dockerfile.storefront`
- `Dockerfile.backend`
- `Dockerfile.cms`
